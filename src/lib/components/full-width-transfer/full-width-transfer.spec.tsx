import React from 'react'
import { render } from '@testing-library/react'
import FullWidthTransfer from './full-width-transfer'

describe('FullWidthTransfer', () => {
  it('should render', () => {
    render(<FullWidthTransfer />)
  })
})
